# GNU Make workspace makefile autogenerated by Premake

.NOTPARALLEL:

ifndef config
  config=debug
endif

ifndef verbose
  SILENT = @
endif

ifeq ($(config),debug)
  serialize_config = debug
  error_config = debug
endif
ifeq ($(config),release)
  serialize_config = release
  error_config = release
endif
ifeq ($(config),dist)
  serialize_config = dist
  error_config = dist
endif

PROJECTS := serialize error

.PHONY: all clean help $(PROJECTS) 

all: $(PROJECTS)

serialize:
ifneq (,$(serialize_config))
	@echo "==== Building serialize ($(serialize_config)) ===="
	@${MAKE} --no-print-directory -C . -f serialize.make config=$(serialize_config)
endif

error: serialize
ifneq (,$(error_config))
	@echo "==== Building error ($(error_config)) ===="
	@${MAKE} --no-print-directory -C . -f error.make config=$(error_config)
endif

clean:
	@${MAKE} --no-print-directory -C . -f serialize.make clean
	@${MAKE} --no-print-directory -C . -f error.make clean

help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  debug"
	@echo "  release"
	@echo "  dist"
	@echo ""
	@echo "TARGETS:"
	@echo "   all (default)"
	@echo "   clean"
	@echo "   serialize"
	@echo "   error"
	@echo ""
	@echo "For more information, see https://github.com/premake/premake-core/wiki"